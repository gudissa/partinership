# Example env for frontend (Vite)
# Set VITE_API_URL to change the API base URL (include protocol and host, omit trailing /api/v1)
# For example to point to 192.168.1.100:5000 set:
# VITE_API_URL=http://192.168.1.100:5000

VITE_API_URL=http://localhost:5000
